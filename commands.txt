list of commands:

npm init -y

npm install

npm install redux 

npm install redux react-redux

npm install @reduxjs/toolkit


useSelector() is subscription

useDispatch() is action

createSlice() is reduxtoolkit function